---
/**:
  ros__parameters:
      # This sets the iface_name in the hw_inventory.json
      # sudo slcand -o -s6 -t hw -S 3000000 /dev/ttyUSBx
      # sudo ip link set up slcan0
      # One sensor per interface
      master_inst_serial_type: "port_based"
      master_data_serial_type: "port_based"
      adapters:
          adapter_0:
              hw_type: "eth"
              hw_dev_id: 3
              hw_iface_name: "eth0"
              port: 55555
          